<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.github.josemodi97</groupId>
    <artifactId>pesaflow4j-parent</artifactId>
    <version>0.1.0</version>
  </parent>

  <artifactId>pesaflow4j-maven-plugin</artifactId>
  <packaging>maven-plugin</packaging>

  <name>pesaflow4j-maven-plugin</name>
  <description>
    Maven plugin for PesaFlow4J: scaffolds a placeholder pesaflow4j.properties credentials
    file into your project (mvn io.github.josemodi97:pesaflow4j-maven-plugin:init).
  </description>
  <url>https://github.com/JoseModi97/pesaflow4j</url>

  <properties>
    <maven.plugin-api.version>3.9.9</maven.plugin-api.version>
    <maven.plugin-annotations.version>3.13.1</maven.plugin-annotations.version>
    <maven.plugin-plugin.version>3.13.1</maven.plugin-plugin.version>
    <maven.core.version>3.9.9</maven.core.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.apache.maven</groupId>
      <artifactId>maven-plugin-api</artifactId>
      <version>${maven.plugin-api.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.maven.plugin-tools</groupId>
      <artifactId>maven-plugin-annotations</artifactId>
      <version>${maven.plugin-annotations.version}</version>
      <scope>provided</scope>
    </dependency>
    <!-- Provides org.apache.maven.project.MavenProject, used by FrameworkDetector. -->
    <dependency>
      <groupId>org.apache.maven</groupId>
      <artifactId>maven-core</artifactId>
      <version>${maven.core.version}</version>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>

    <!--
      Test-only: the generated framework examples (Pesaflow4jInitMojo /
      FrameworkExample) are verified by actually compiling them with
      javax.tools.JavaCompiler against these real dependency jars - not
      just eyeballed - so a wrong generated import or method signature
      fails the build, not just looks plausible.
    -->
    <dependency>
      <groupId>javax.servlet</groupId>
      <artifactId>javax.servlet-api</artifactId>
      <version>4.0.1</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>jakarta.servlet</groupId>
      <artifactId>jakarta.servlet-api</artifactId>
      <version>6.0.0</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.github.josemodi97</groupId>
      <artifactId>pesaflow4j-servlet</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.github.josemodi97</groupId>
      <artifactId>pesaflow4j-jakarta</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.github.josemodi97</groupId>
      <artifactId>pesaflow4j-spring-boot2-starter</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.github.josemodi97</groupId>
      <artifactId>pesaflow4j-spring-boot3-starter</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>pesaflow4j-maven-plugin-${project.version}</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-plugin-plugin</artifactId>
        <version>${maven.plugin-plugin.version}</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-javadoc-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
